'use strict';

var app = angular.module('app');

app.directive('showTenant', function () {
	return {
		restrict: 'E',
		templateUrl: '<%= asset_path('templates/tenants/show.html') %>',
		controller: 'TenantShowController',
		controllerAs: 'showTenant'
	};
});